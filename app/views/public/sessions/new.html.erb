<div class="container bg-light">
  <div class="row mx-auto">
      <h3 class="col-md-4 my-5 offset-md-2">会員ログイン</h3>
  </div>

  <div class="row mt-3 mx-auto">
    <div class="col-md-8 mt-3 offset-md-3">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <table>
      <div class="field">
        <tr>
          <th><%= f.label :email,"メールアドレス" %></th>
          <th><%= f.email_field :email, autofocus: true, autocomplete: "email", :placeholder => "xxxxxx@email.com" %></th>
        </tr>
      </div>

      <div class="field">
        <tr>
          <th><%= f.label :password,"パスワード" %></th>
          <th><%= f.password_field :password, autocomplete: "current-password" %></th>
        </tr>
      </div>
    </table>

    <% if devise_mapping.rememberable? %>
      <div class="field pt-2">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me,"ログイン状態を保存する" %>
      </div>
    <% end %>
    <div class="actions mt-4">
      <%= f.submit "ログイン",class:"btn btn-primary offset-md-3" %>
    </div>
    <% end %>
    </div>
  </div>

  <div class="row  offset-md-3 mt-5">
      <h5>新規会員登録がお済みでない方</h5>
  </div>
  <div class="row  offset-md-3">
      <p>新規会員登録がお済みではない方は<%= render "public/shared/links" %>から新規会員登録をしてください。</p>
  </div>




</div>
