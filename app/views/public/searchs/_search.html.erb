<% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url: search_path, local: true do |f| %>
      <%= f.select :search, options_for_select([['ログイン名'], ['タグ']]) %>
      <%= f.text_field :word %>
      <%= f.submit "検索", class: "btn btn-primary" %>
    <% end %>
  </div>
<% end %>